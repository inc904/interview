# Vue中的响应式

vue中的响应式指的是**Vue自动追踪数据变化并更新视图的底层机制**。当修改数据时，使用该数据的网页内容会自动改变，不需要手动操作网页标签。

## 核心原理

- 依赖追踪：当读取数据时，系统会记住是谁在用它。
- 变更拦截：当修改数据时，系统能马上知道谁改变了。
- 自动触发：数据变了之后，系统自动通知页面重新渲染。

## 不同版本实现

- Vue2 中使用 `Object.defineProperty` 拦截对象属性的读取和修改。
- Vue3 中使用Javascript中的`Proxy`代理对象，性能更好，功能更强。
