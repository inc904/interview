# 数组扁平化

```js
const arr = [1, 2, [3, 4, [5, 6]]]

// flat es2019 原生
const flatArr = arr.flat(Infinity)

// reduce 递归
const flatArr2 = arr.reduce((pre, cur) => {
  return Array.isArray(cur) ? acc.concat(flatArr(cur)) : acc.concat(cur)
}, [])

// 展开运算符
const flatArr3 = arr.reduce((pre, cur) => {
  return [...pre, ...(Array.isArray(cur) ? flatArr(cur) : [cur])]
})
```
